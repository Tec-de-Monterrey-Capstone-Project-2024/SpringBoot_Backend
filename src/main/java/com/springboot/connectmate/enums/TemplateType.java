package com.springboot.connectmate.enums;

public enum TemplateType {
    // Used for metrics where a higher value indicates better performance.
    POSITIVE_UPSIDE,
    //  Used for metrics where a higher value indicates worse performance.
    NEGATIVE_UPSIDE

}

