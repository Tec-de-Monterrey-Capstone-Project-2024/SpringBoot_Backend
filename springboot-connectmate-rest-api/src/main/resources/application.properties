# Application name
spring.application.name=springboot-connectmate-rest-api

# MySQL DB Configuration
<<<<<<< Updated upstream
spring.datasource.url=jdbc:mysql://localhost:3306/connectmate?useSSL=false
spring.datasource.username=USER
spring.datasource.password=PASSWORD

# Hibernate ddl auto (create, create-drop, update, ...)
spring.jpa.hibernate.ddl-auto=update

# Show Logs (only for development)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
=======
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:connectmate}
spring.datasource.username=${DB_USER:USER}
spring.datasource.password=${DB_PASSWORD:PASSWORD}

# hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Hibernate ddl auto (create, create-drop, update, ...)
spring.jpa.hibernate.ddl-auto=update
>>>>>>> Stashed changes
